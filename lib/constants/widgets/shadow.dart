import 'package:flutter/material.dart';

class GShadowStyle {
  static final VerticalProductShadow = BoxShadow(
    color: Colors.black54.withOpacity(0.1),
    blurRadius: 50,
    spreadRadius: 7,
    offset: Offset(0, 2),
  );

  static final HorizontalProductShaow = BoxShadow(
    color: Colors.black54.withOpacity(0.1),
    blurRadius: 50,
    spreadRadius: 7,
    offset: Offset(0, 2),
  );
}
